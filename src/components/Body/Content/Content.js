import React from 'react';
import './Content.css';

const content = (props) => (
    <div className="content-box">
        <img src={props.image} alt="..." />    
        </div>
);

export default content;